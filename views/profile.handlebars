<section>

  <header class="header">
    <p class="name">TaskPro</p>

    <nav class="nav">
      <a
        href="{{#if is_manager}}/managerDashboard{{else if is_supervisor}}/supervisorDashboard{{else}}/crewDashboard{{/if}}">
        Dashboard
      </a>
      <a id="logout">logout</a>
    </nav>
  </header>

  <main>
    <!-- display the users name, member of *crew number* -->
    <div class="profileContainer">
      <h1 class="profileTitle">{{first_name}}'s Profile</h1>
      <p>Group #{{crew_id}}</p>
      <p class="emailText">Work Email: {{email}}</p>
    </div>

    <div class="card-container">
      <div class="card">
        <h2>Completed Tasks</h2>
        <div class="card-content">
          <ul>
            <li>Task one</li>
            <li>Task two</li>
            <li>Task three</li>
          </ul>
        </div>
      </div>
    </div>

  <!-- number of completed tasks -->

  <!-- username -->

  <!-- email if provided-->
  </main>

  <script type="module" src="../public/js/logout.js"></script>

</section>
